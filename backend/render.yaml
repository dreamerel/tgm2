services:
  - type: web
    name: tg-manager-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host=0.0.0.0 --port=$PORT
    envVars:
      - key: JWT_SECRET
        generateValue: true
    plan: free
